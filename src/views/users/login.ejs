<!-- head added -->
<%- include('../partials/head'); %>

<!-- header added -->
<%- include('../partials/header'); %>

        <main>
        <div class="container-login">
            <div class="container-login-logo">
            <img src="\images\logos\user-icon.png">
            </div>
            <form class="container-form" action="/login" method="post">
                <div class="container-inputs">
                    <div class="login-container-email">
                            <input type=" email" name="email" placeholder="E-mail" class="login-email">
                            <% if (locals.errors && errors.email) { %>
                                <div>
                                  <%= errors.email.msg %>
                                </div>
                              <% } %>
                    </div>
                    <div class="login-container-pass">
                        <input type="password" name="password" placeholder="Contraseña" class="login-pass">
                    </div>
                    <div class="login-remember-father switches">
                        <input type="checkbox" id="1">
                        <label for="1">
                         <span></span>
                       </label>  Recordar</label>

                    </div>

                </div>
                <div class="login-submit">
                    <input type="submit" class="button-login-submit" value="Ingresar">
                </div>
                <div class="login-register">
                    ¿No tienes cuenta? <a href="register">Registrate</a>
                </div>
                <div class="login-forget">
                    <a href="forget">¿Olvidaste tu contraseña?</a>
                </div>
            </form>
        </div>
</main>
<!-- footer added -->
<%- include('../partials/footer'); %>